class AppConstants {
  static const String appName = 'ResQ';
  static const String tagline = 'Respond. Resolve. Reassure.';
  static const String poweredBy = 'Powered by ResQ Technologies';
  static const Duration splashDuration = Duration(milliseconds: 2500);
  static const Duration holdDuration = Duration(seconds: 3);
  static const Duration silentPanicDuration = Duration(seconds: 2);
  static const Duration pulseDuration = Duration(milliseconds: 1500);
  
  // SharedPreferences keys
  static const String userTypeKey = 'user_type';
  static const String staffType = 'staff';
  static const String guestType = 'guest';
}
